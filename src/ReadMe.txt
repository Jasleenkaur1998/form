State:
const [valueOne,valueTwo]= useState(default value);

onClick={} //for buttons
onChange={} //for input tags

onChange return an event through  which we capture enetered values inside the input box